[phases.setup]
nixPkgs = ["python313", "python313Packages.pip", "python313Packages.virtualenv"]

[phases.install]
cmds = ["python3 -m venv /app/venv", "/app/venv/bin/pip install -r requirements.txt"]

[start]
cmd = "cd zynex_cartel && /app/venv/bin/python main.py"
